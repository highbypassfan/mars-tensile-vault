# Blender backup files and local render outputs
*.blend1
*.blend2
__pycache__/
renders/
*.log
.DS_Store
Thumbs.db

# The blend references these two sky maps externally, so they must be committed.
# The older combined 4k map is packed/unused and stays local.
assets/sky/starmap_2020_4k.exr

# Surface image source copies are packed into the blend
assets/surfaces/*.jpg

# Local scene-development previews
previews/*-test.png

# Owner-created local lighting variants
mars-tensile-vault_night.blend
mars-tensile-vault_nightnorooflts.blend
